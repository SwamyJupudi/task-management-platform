/**
 * The dashboard feature's public surface.
 *
 * The router needs the screen and nothing else. The hooks, the API calls and
 * the panels are internals and stay that way, so the feature is free to change
 * shape without anything outside it noticing.
 */

export { DashboardPage } from './pages/dashboard-page'
