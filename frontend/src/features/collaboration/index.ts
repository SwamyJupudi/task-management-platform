/**
 * The collaboration feature's public surface.
 *
 * One panel: the discussion, the files and the history on a task, which the
 * task detail screen mounts. Everything else — the API calls, the hooks, the
 * mention format and the individual panels — is internal and stays that way.
 */

export { CollaborationPanel } from './components/collaboration-panel'
