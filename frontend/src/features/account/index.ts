/**
 * The account feature's public surface.
 *
 * The router needs the three screens and nothing else. The hooks, the API calls
 * and the shell are internals and stay that way.
 */

export { AccountProfilePage } from './pages/profile-page'
export { AccountPasswordPage } from './pages/password-page'
export { AccountSessionsPage } from './pages/sessions-page'
