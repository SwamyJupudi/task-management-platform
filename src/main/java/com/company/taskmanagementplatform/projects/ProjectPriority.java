package com.company.taskmanagementplatform.projects;

/** The four levels the requirements name. Shared with tasks when they arrive. */
public enum ProjectPriority {
    LOW,
    MEDIUM,
    HIGH,
    CRITICAL
}
