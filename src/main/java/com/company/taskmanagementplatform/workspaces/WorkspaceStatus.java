package com.company.taskmanagementplatform.workspaces;

/** Whether a workspace is in use. The wider lifecycle arrives with the workspace phase. */
public enum WorkspaceStatus {
    ACTIVE,
    ARCHIVED
}
